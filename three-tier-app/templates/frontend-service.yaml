apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: {{ .Values.environment }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ .Values.frontend.nodePort }}
  selector:
    app: frontend
