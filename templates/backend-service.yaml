apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: {{ .Values.environment }}
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: {{ .Values.backend.port }}
  selector:
    app: backend
