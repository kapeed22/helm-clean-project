apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: {{ .Values.environment }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: {{ .Values.redis.port }}
  selector:
    app: redis
