apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: {{ .Values.environment }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: {{ .Values.postgres.port }}
  selector:
    app: postgres
